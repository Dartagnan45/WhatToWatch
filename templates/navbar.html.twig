<nav class="navbar navbar-expand-lg navbar-default navbar-full">
	<a class="navbar-brand" href="{{path("/")}}">WhatToWatch</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav">
			<li class="nav-item dropdown">
				<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
					Listes de films
				</a>
				<div class="dropdown-menu" aria-labelledby="navbarDropdown">
					{% for key,genre in genres %}
						{% if key != 'series' %}
							<a class="dropdown-item" href="{{ path("movies_show",{ 'list': genre }) }}">{{ key }}</a>
						{% endif %}
					{% endfor %}
				</div>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path("movies_show",{ 'list': genres.series }) }}">Séries</a>
			</li>
		</ul>
	</div>
</nav>
